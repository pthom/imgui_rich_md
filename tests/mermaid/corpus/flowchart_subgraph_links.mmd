%% Links to and from a subgraph: they end on its box (one declared before the subgraph)
%% expect: nodes=5 edges=5 subgraphs=2
%% known: edge-overlap (the dashed link's approach line runs next to the channel: the gap is at its least)
flowchart TD
    Client --> Backend
    subgraph Backend
        Api[API] --> Db[(Database)]
    end
    Backend --> Monitor
    subgraph Ops
        Monitor --> Alerts
    end
    Client -.-> Ops
